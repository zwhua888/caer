IF (NOT ENABLE_DATASYNCH)
	SET(ENABLE_DATASYNCH 0 CACHE BOOL "Enable the sychronization of multiple stream of input data")
ENDIF()

IF (ENABLE_DATASYNCH)
	SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_DATASYNCH=1 PARENT_SCOPE)

	SET(CAER_DATASYNCH_FILES modules/datasynch/datasynch.c)

	SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_DATASYNCH_FILES} PARENT_SCOPE)
ENDIF()
